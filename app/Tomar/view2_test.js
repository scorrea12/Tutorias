'use strict';

describe('myApp.Tomar module', function() {

  beforeEach(module('myApp.Tomar'));

  describe('Tomar controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var view2Ctrl = $controller('View2Ctrl');
      expect(view2Ctrl).toBeDefined();
    }));

  });
});